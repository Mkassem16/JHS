### Variables are named in the code book 
1) loading all data provided 
2) binding rows of traing and test data
3) binding rows of labels data
4) binding columns of training and labels
5) using features as column names through function to extract first item of a list 
6) detecting columns with mean and std values through grep function 
7) selecting df with selected columns only 
8) grouping df by activity 
9) tidy df summarizing averages for activities variables 
